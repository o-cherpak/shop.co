import type { Product } from "../../interfaces/Products";

type ProductSectionProps = {
  product: Product;
};

export function ProductSection({ product }: Readonly<ProductSectionProps>) {
  return (
    <section>
      
    </section>
  );
}
